.login-page { padding: 120rpx 48rpx 0; text-align: center; }
.logo { font-size: 100rpx; }
.l-title { font-size: 44rpx; font-weight: 800; margin-top: 20rpx; }
.l-sub { color: var(--ink-2); font-size: 26rpx; margin-top: 8rpx; }
.hint { font-size: 22rpx; color: var(--ink-3); margin-top: 24rpx; }
.back { margin-top: 40rpx; color: var(--ink-2); font-size: 26rpx; }
